package utils

object ConfPath {
  object Line {
    final val ChannelSecretPath = "line.channel.secret"
    final val ReplyUrlPath = "line.reply.url"
    final val AccessToken = "line.access.token"
  }
}
